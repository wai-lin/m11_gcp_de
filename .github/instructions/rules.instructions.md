---
description: Refer when installing new package
# applyTo: 'Refer when installing new package' # when provided, instructions will automatically be added to the request context when the pattern matches an attached file
---

# Install using `uv`

## Commands

- `uv add <package-name>`: Installs a package.
- `uv run <file-name>.py`: Runs a python script.
